!!!
html
  head
    include includes/head

  body
    block header
      #header
        if session.email
          p Logged in as #{session.email}
          a(href='/logout') Log out
        else
          a(href='#', id='login') Log in with BrowserID
          form(method='post', action='/login')
            input(type='hidden', name='bid_assertion', value='1')

    block content

    block footer
